# Demo Links TODO

The portfolio no longer shows `LIVE` buttons by default. Add demo links only when there is a real destination worth showing.

## Rules

- Use `LIVE` only for a real deployed URL.
- Use `LOCAL DEMO` only if the case study page includes run commands or a short video/GIF.
- Use `VIDEO` for a screen recording when the stack is too heavy for recruiters to run.
- Keep `SOURCE` for public repos; use `PRIVATE REPO` or hide it when source is not public.
- Keep `CASE STUDY` for the internal portfolio detail page.

## Project Demo Ideas

| Project | Future demo target |
|---|---|
| AI Assistant Across Websites | Browser extension panel summarizing a page, saving a note, and searching memory. |
| AI Candidate Dashboard | Resume upload, processing state, recruiter dashboard, candidate detail. |
| AI Inbox Copilot | Inbox thread summary, draft reply, and reminder flow. |
| AIHL B2B Platform | Mobile account/transactions plus admin dashboard walkthrough. |
| AIHL Secure Service Booking | Service browse, provider profile, slot selection, booking lifecycle. |
| Error Monitoring System | Ingest a sample error, show dashboard/log detail, trigger alert rule. |
| Clipsync | Terminal or video demo of clipboard sync between two clients. |
| CityPulse | Map search, layer toggles, weather/air-quality/sidebar chart walkthrough. |
| Crypto Market Data Pipeline | Trigger ingestion, show latest market table/chart and warehouse output. |
| Spark Data Pipeline | Run ETL stage and show Streamlit/dashboard or output summary. |
| Task Tracker Spring + Angular | Login, create/edit task, filter dashboard, update status. |
| Customer Issue Tracker | Customer submits an issue, analyst triages it, admin views reporting. |
| Java Banking System | Start server/client, account actions, transaction history. |
| JVM Tuning Benchmark | Benchmark script plus p99 latency comparison/Grafana snapshot. |
| Multi-Source ETL | Pipeline run, validation/quarantine output, Power BI/report preview. |
| NoteMind | iOS simulator note CRUD, offline sync, streaming weekly summary. |
| Oil & Gas Dashboard | Power BI report preview with operator/well filters and forecast page. |
| Pre-Interview Audio | Audio upload, processing state, transcript/summary/signals detail. |
| Web Builder LLM | Prompt-to-site generation, preview, and modify-existing flow. |
